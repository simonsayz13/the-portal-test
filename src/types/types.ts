export type ItemData = {
  product_code: string;
  quantity: string;
  pick_location: string;
};

export type ItemInfo = {
  product_code: string;
  quantity: string;
  pick_location: string;
  bay: string;
  shelf: number;
};
